
@{
    ViewBag.Title = "leerDatos";
}

<h2>leerDatos</h2>

@foreach (var item in ViewBag.Archivos)
            {
                <ul>
                    @foreach(var item2 in item.Split(','))
                {
                     <li>
                         <h3>@item2</h3>
                     </li>
                    
                }
                </ul>
            }

